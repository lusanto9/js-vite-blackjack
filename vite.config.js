import { defineConfig } from 'vite';

// Exportamos la configuración de Vite que recibe un objeto
export default defineConfig({

    base: '/blackjack-vite/', // Ruta base para el despliegue en GitHub Pages

})