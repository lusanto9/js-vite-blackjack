import { defineConfig } from 'vite';

// Exportamos la configuración de Vite que recibe un objeto
export default defineConfig({

    base: '/js-vite-blackjack/', // Ruta base para el despliegue en GitHub Pages

})